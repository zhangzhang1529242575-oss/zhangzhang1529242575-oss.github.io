---
title: "重复控制器（Repetitive Control）概述"
date: 2026-06-02
math: true
tags: ["重复控制", "谐波抑制", "滑模控制", "电机控制"]
keywords: ["重复控制", "RC", "谐波抑制", "内模原理", "转矩脉动"]
summary: "重复控制器基于内模原理，专门用于跟踪和抑制周期性信号。本文介绍其核心原理、离散实现、与滑模控制的互补关系，以及在集成绕组双端口电机谐波抑制中的应用前景。"
---

## 核心原理

重复控制基于**内模原理（Internal Model Principle）**，专门用于跟踪/抑制**周期性信号**。其核心思想是：

> 如果一个周期为 $T$ 的周期性扰动出现了一次，它将在下一个周期再次出现——控制器"记住"上一个周期的误差，并在当前周期产生补偿。

在频域中，重复控制器的核心是一个**周期信号发生器**：

$$G_{rc}(s) = \frac{e^{-sT}}{1 - e^{-sT}}$$

这个传递函数在基频 $\omega_0 = \frac{2\pi}{T}$ 及其所有整数倍频率 $k\omega_0$ 处具有**无穷大增益**，从而实现对周期性信号的无静差跟踪或完全抑制。

## 离散实现

在实际数字控制（DSP/微控制器）中，重复控制器通常实现为：

$$G_{rc}(z) = \frac{z^{-N}}{1 - Q(z)\,z^{-N}} \cdot C(z)$$

其中：
- $N = \dfrac{T}{T_s}$ —— 一个信号周期内的采样点数
- $Q(z)$ —— 低通滤波器或略小于 1 的常数（如 0.95），用于增强鲁棒性（以微小稳态误差换取稳定性）
- $C(z)$ —— 补偿器（相位超前 + 幅值补偿）

## 与集成绕组双端口电机的关联

| 重复控制的特点 | 与本研究的关联 |
|---|---|
| 谐波频率处无穷增益 | 直接针对转矩脉动的 $6k$ 次谐波（5、7、11、13…） |
| 周期性扰动抑制 | 齿槽转矩、反电动势谐波、死区效应都是周期性的 |
| 无需精确被控对象模型 | 对参数变化鲁棒，与鲁棒控制有理论交集 |
| 计算量：需存储 $N$ 个历史误差值 | 高转速下 $N$ 小，低转速下 $N$ 大，需权衡内存 |

## 与滑模控制的对比与互补

|  | 重复控制（RC） | 滑模控制（SMC） |
|---|---|---|
| **强项** | 稳态周期性扰动抑制（谐波抑制） | 瞬态快速响应、参数鲁棒性、抗干扰 |
| **弱项** | 暂态响应慢（延迟一个周期）；非周期扰动无效 | 抖振现象；对周期性谐波的针对性不如 RC |
| **适用场景** | 稳态运行、恒速/缓变工况 | 启动、变速、负载突变工况 |

> **理想复合策略：RC + SMC 并联/级联**
>
> SMC 处理瞬态响应 + 参数变化，RC 处理稳态谐波抑制。两者在时频域上互补，可同时获得快速动态和低谐波畸变。

## 常见改进变体

### 1. 奇次谐波重复控制（Odd-Harmonic RC）

$$G_{rc,odd}(z) = \frac{-z^{-N/2}}{1 + z^{-N/2}}$$

只抑制奇次谐波（5、7、11、13…），**延迟减半**（$N/2$ 而非 $N$），动态响应更快。非常适合三相电机——因为对称结构中偶次谐波通常不存在。

### 2. $nk \pm m$ 次谐波重复控制

针对特定谐波族（如双端口机器的特定谐波模式）。

### 3. 分数阶重复控制

当采样频率不是基频的整数倍时（变频驱动中常见）。

### 4. 自适应重复控制

频率自适应——对于变频电机驱动非常有用。

## 关键文献

- **Hara et al. (1988)** — 重复控制的奠基性工作
- **Tomizuka et al.** — 离散重复控制的改进（$Q$ 滤波器的引入）
- **Mattavelli et al.** — 重复控制在电力电子和电机驱动中的应用
- **Tang et al. / Chen et al.** — RC + SMC 复合控制策略在 PMSM 中的应用
